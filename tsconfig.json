{
    "extends": "@workleap/typescript-configs/monorepo-root.json",
    "exclude": ["packages", "sample", "public", "node_modules"],
    // Prevent TS18003 since there are no TS files at the root at the moment.
    "references": []
}
