{
    "extends": "@workleap/typescript-configs/monorepo-workspace.json",
    "exclude": ["packages", "samples", "templates", "node_modules"],
    // Prevent TS18003 since there are no TS files at the root at the moment.
    "references": []
}
